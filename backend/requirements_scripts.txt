# Dépendances additionnelles pour les scripts d'optimisation GlobeGenius
# Ces dépendances s'ajoutent à requirements.txt principal

# Affichage coloré dans le terminal
colorama==0.4.6

# Graphiques et visualisation (optionnel mais utile)
matplotlib==3.7.1
seaborn==0.12.2

# Tableaux formatés dans le terminal
tabulate==0.9.0

# Barre de progression
tqdm==4.65.0

# Analyse de données avancée
scipy==1.11.4  # Déjà dans requirements.txt mais on s'assure
numpy==1.26.3  # Déjà dans requirements.txt mais on s'assure

# Pour les graphiques ASCII dans le terminal (optionnel)
asciichartpy==1.5.25

# Gestion avancée du terminal
blessed==1.20.0